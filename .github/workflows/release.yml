name: Release

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      - name: Install wasm-pack and wasm-opt
        run: |
          which wasm-pack || cargo install wasm-pack
          which wasm-opt || cargo install wasm-opt

      - name: Cache Cargo dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-tools


      - name: Release Build (Workspace)
        run: cargo build --release --workspace

      - name: Run Tests (Parser with locations)
        run: cargo test -p sevenmark-parser --features include_locations -- --nocapture

      - name: Run Tests (Transform)
        run: cargo test -p sevenmark-transform --features sevenmark-parser/include_locations -- --nocapture

      - name: Run Tests (Server)
        run: cargo test -p sevenmark-server -- --nocapture

      - name: Add WASM target
        run: rustup target add wasm32-unknown-unknown

      - name: Test WASM build (Web)
        working-directory: ./sevenmark-transform
        run: wasm-pack build --target web --features wasm

      - name: Test WASM build (Bundler)
        working-directory: ./sevenmark-transform
        run: wasm-pack build --target bundler --features wasm

      - name: Test WASM build (Node.js)
        working-directory: ./sevenmark-transform
        run: wasm-pack build --target nodejs --features wasm