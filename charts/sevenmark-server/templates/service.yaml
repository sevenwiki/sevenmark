apiVersion: v1
kind: Service
metadata:
  name: {{ include "sevenmark_server.fullname" . }}
  labels:
    {{- include "sevenmark_server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sevenmark_server.selectorLabels" . | nindent 4 }}
