services:
  sevenmark-server:
    build: .
    container_name: sevenmark-server
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - docker.env
    ports:
      - "9000:9000"